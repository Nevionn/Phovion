/**
 * Не используйте этот пароль для полноценной защиты приложения
 * Пароль используется формально, от "лишних глаз" при попадания в интерфейс
 *
 * Ввод пароля будет запрашиваться при каждом старте приложения
 */

export const UI_PASS: string | null = null;
